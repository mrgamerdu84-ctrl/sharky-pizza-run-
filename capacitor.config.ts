import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tikowiko.sharkypizzarun',
  appName: 'Sharky Pizza Run',
  webDir: 'dist',
  android: {
    backgroundColor: '#111827',
  },
};

export default config;
